apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: issuematch-postgres-pgdata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: issuematch
  labels:
    app: issuematch
spec:
  containers:
    - name: postgres
      image: docker.io/library/postgres:18
      env:
        - name: POSTGRES_DB
          value: issuematch
        - name: POSTGRES_USER
          value: issuematch
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: issuematch-secrets
              key: postgres_password
      volumeMounts:
        - name: pgdata
          mountPath: /var/lib/postgresql
    - name: issuematch
      image: localhost/issuematch:latest
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        seccompProfile:
          type: RuntimeDefault
      ports:
        - containerPort: 9473
      env:
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: issuematch-secrets
              key: session_secret
        - name: ADMIN_USERNAME
          valueFrom:
            secretKeyRef:
              name: issuematch-secrets
              key: admin_username
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: issuematch-secrets
              key: admin_password
        - name: DB_HOST
          value: localhost
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: issuematch
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: issuematch-secrets
              key: postgres_password
        - name: DB_NAME
          value: issuematch
  volumes:
    - name: pgdata
      persistentVolumeClaim:
        claimName: issuematch-postgres-pgdata
---
apiVersion: v1
kind: Service
metadata:
  name: issuematch
spec:
  selector:
    app: issuematch
  ports:
    - port: 9473
      targetPort: 9473
